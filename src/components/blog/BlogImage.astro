---
interface Props {
    src?: string;
    alt: string;
}

const { src, alt } = Astro.props;
---

{src && (
    <img
        src={src}
        alt={alt}
        class="w-full h-[400px] object-cover rounded-lg"
    />
)}