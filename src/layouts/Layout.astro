---
import BaseLayout from "./BaseLayout.astro";

const { title } = Astro.props;
---

<BaseLayout title={title} layoutType="default">
    <div class="max-w-8xl mx-auto px-6 lg:px-12 mt-4 sm:mt-16">
        <slot />
    </div>
</BaseLayout>
